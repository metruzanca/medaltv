fn main() {
    println!("medaltv — unofficial medal.tv API client");
    println!();
    println!("Examples:");
    println!("  cargo run --example get_user           — look up a user profile");
    println!("  cargo run --example get_category       — fetch a category by ID");
    println!("  cargo run --example get_recent_games   — list recent gaming sessions");
    println!("  cargo run --example get_recent_clips   — list recent clips");
}
